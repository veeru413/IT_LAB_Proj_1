# ---------------------------------------------------------------------------
# Frontend environment (optional)
#
# By default the client calls the relative path "/api" and Vite proxies it to
# the Express server on port 5000 (see vite.config.ts). You only need this file
# if the API lives somewhere else - e.g. a deployed build.
# ---------------------------------------------------------------------------

# VITE_API_URL="http://localhost:5000/api"
